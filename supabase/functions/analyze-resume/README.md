Set these secrets before deploying:

- `OPENAI_API_KEY`
- `OPENAI_MODEL` (optional, default: `gpt-4o-mini`)

Deploy with:

`supabase functions deploy analyze-resume --project-ref ecpvghewdvhypcfzvuqv`

